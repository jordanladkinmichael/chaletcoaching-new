# План реализации расширенной FAQ страницы

## 📋 Анализ текущего состояния

### Существующее:
- ✅ `app/faq/page.tsx` - простая страница с 5 вопросами
- ✅ `lib/faq-data.ts` - базовая структура FAQ (10 вопросов, без категорий)
- ✅ `components/ui/accordion.tsx` - компонент Accordion готов
- ✅ `components/ui/input.tsx` - SearchInput компонент готов
- ✅ `lib/hooks.ts` - useDebounce hook готов
- ✅ `components/ui/container.tsx` - Container компонент (max-w-[1200px])
- ✅ Страницы `/support`, `/payments-tokens`, `/trust-safety`, `/contact` существуют

### Что нужно сделать:
1. Расширить `lib/faq-data.ts` с категориями и keywords (24 вопроса)
2. Переделать `app/faq/page.tsx` согласно требованиям
3. Добавить поиск и фильтрацию
4. Реализовать 2-колоночный layout
5. Добавить quick links и CTA секцию

---

## 🎯 План действий

### Этап 1: Расширение FAQ данных (lib/faq-data.ts)

**Задачи:**
1. Добавить типы `FaqCategory` и обновить `FaqItem` с полем `keywords`
2. Создать 24 FAQ вопроса согласно требованиям:
   - Getting started (3 вопроса)
   - Coaches (4 вопроса)
   - Instant AI (4 вопроса)
   - Tokens & payments (5 вопросов)
   - Account (3 вопроса)
   - Safety (3 вопроса)
   - Refunds (2 вопроса)
3. Сохранить обратную совместимость с существующим кодом (MINI_FAQS, coachProfileFAQ)

**Файлы:**
- `lib/faq-data.ts` (обновить)

---

### Этап 2: Создание FAQ страницы (app/faq/page.tsx)

**Задачи:**
1. Hero секция:
   - H1: "Frequently asked questions"
   - Subtitle: "Quick answers about coaches, Instant AI plans, tokens, and safety."
   - SearchInput с placeholder "Search questions..."
   - Helper text: "Popular: tokens, preview, refunds, dashboard"
   - Quick links: Support, Payments & tokens, Trust & safety

2. Category chips:
   - Кнопки-чипсы: All, Getting started, Coaches, Instant AI, Tokens & payments, Account, Safety, Refunds
   - Активное состояние
   - Фильтрация по категориям

3. FAQ контент:
   - 2-колоночный layout на desktop (md:grid-cols-2)
   - 1 колонка на mobile
   - Использовать Accordion компонент
   - Разделить отфильтрованные вопросы пополам для колонок
   - Добавить padding-top для ответов

4. "Still have questions?" CTA:
   - Card с заголовком и текстом
   - Кнопки: "Open support" (primary) и "Contact us" (secondary)

5. Интеграция:
   - Использовать SiteHeader и SiteFooter
   - Использовать Container для ширины (как на Home)
   - Использовать useDebounce для поиска (150ms)
   - Client-side фильтрация по question, answer, keywords

**Файлы:**
- `app/faq/page.tsx` (полностью переделать)

---

### Этап 3: Стилизация и UX

**Задачи:**
1. Стили для category chips:
   - Активное состояние (primary цвет)
   - Hover эффекты
   - Focus states (WCAG)

2. Empty state для "No results":
   - Сообщение
   - CTAs к `/support` и `/contact`

3. Анимации:
   - Плавное открытие/закрытие Accordion (уже есть)
   - Плавная фильтрация (если нужно)

4. Адаптивность:
   - Mobile: 1 колонка
   - Desktop: 2 колонки
   - Правильные breakpoints

**Файлы:**
- `app/faq/page.tsx` (стили)

---

## 🔍 Вопросы для уточнения

1. **Существующие FAQ:**
   - Сохранить существующие FAQ из `lib/faq-data.ts` или полностью заменить новыми 24 вопросами?
   - Нужно ли обновить MINI_FAQS на Home странице?

2. **Ссылки:**
   - Quick links ведут на `/support`, `/payments-tokens`, `/trust-safety` - все страницы существуют ✅
   - CTA кнопки ведут на `/support` и `/contact` - существуют ✅

3. **Стили:**
   - Использовать те же стили, что на Support странице, или создать уникальные?
   - Category chips - использовать Badge компонент или создать кастомные кнопки?

4. **Контент:**
   - Нужно ли переводить на другие языки или только EN?
   - Нужны ли ссылки в ответах (кроме refund policy)?

---

## 📝 Детальный план реализации

### Шаг 1: Обновить lib/faq-data.ts
```typescript
// Добавить типы
export type FaqCategory = 
  | "getting_started"
  | "coaches"
  | "instant_ai"
  | "tokens_payments"
  | "account"
  | "safety"
  | "refunds";

export interface FaqItem {
  id: string;
  category: FaqCategory;
  question: string;
  answer: string;
  keywords: string[];
}

// Создать EXPANDED_FAQS с 24 вопросами
// Сохранить MINI_FAQS и coachProfileFAQ для обратной совместимости
```

### Шаг 2: Создать app/faq/page.tsx
```typescript
// Структура:
1. SiteHeader
2. Container
   - Hero (H1, subtitle, search, quick links)
   - Category chips
   - FAQ content (2 columns)
   - CTA section
3. SiteFooter

// Логика:
- useState для search query
- useState для selected category
- useDebounce для search (150ms)
- useMemo для фильтрации FAQ
- Разделение на 2 колонки
```

### Шаг 3: Стилизация
- Category chips с активным состоянием
- Empty state
- Правильные отступы и spacing
- Адаптивность

---

## ✅ Критерии готовности

- [ ] FAQ страница доступна по `/faq`
- [ ] 24 вопроса отображаются
- [ ] Поиск работает (фильтрация по question, answer, keywords)
- [ ] Фильтрация по категориям работает
- [ ] 2-колоночный layout на desktop, 1 колонка на mobile
- [ ] Accordion работает плавно (180-220ms)
- [ ] Quick links ведут на правильные страницы
- [ ] CTA секция с кнопками работает
- [ ] Empty state показывается при отсутствии результатов
- [ ] Ширина страницы совпадает с Home (Container)
- [ ] Header и Footer идентичны Home
- [ ] WCAG 2.2 AA соблюдено (focus states, headings order)
- [ ] Ссылки на `/legal/refunds` работают

---

## 🚨 Важные замечания

1. **Обратная совместимость:**
   - Сохранить MINI_FAQS для Home страницы
   - Сохранить coachProfileFAQ для профилей коучей
   - Добавить EXPANDED_FAQS для новой страницы

2. **Производительность:**
   - Client-side фильтрация (быстро)
   - Debounce для поиска (150ms)
   - useMemo для фильтрованных результатов

3. **Доступность:**
   - Правильный порядок заголовков (h1 → h2 → h3)
   - Focus states на всех интерактивных элементах
   - ARIA labels где нужно
   - Keyboard navigation

---

## 📦 Файлы для изменения

1. `lib/faq-data.ts` - расширить с категориями и keywords
2. `app/faq/page.tsx` - полностью переделать

---

**Готов к реализации после вашего одобрения!**

