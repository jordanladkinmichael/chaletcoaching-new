# Chaletcoaching

Private project repository for Chaletcoaching.

Minimal public details by design.

## Local setup

- Do **not** commit `.env` or `.env.local` (they are in `.gitignore`). These files may contain API keys and secrets.
- If a secret was ever pushed, rotate that key in the provider (e.g. OpenAI, NextAuth) and update it locally.
