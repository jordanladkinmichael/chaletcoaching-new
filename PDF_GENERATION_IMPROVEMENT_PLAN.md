# План улучшения генерации PDF курсов

## 📋 Обзор

**Цель:** Оптимизировать генерацию PDF курсов с ограничением в 4 страницы, улучшить качество контента и производительность.

**Текущий стек:**
- Vercel (хостинг)
- Neon DB (PostgreSQL)
- Inngest (очередь задач)
- Puppeteer + Chromium (рендеринг PDF)
- OpenAI API (генерация контента)

**Ограничение:** PDF максимум 4 страницы

---

## 💾 Хранение PDF файлов

### Рекомендация: **Vercel Blob Storage**

**Почему не Neon DB:**
- ❌ PostgreSQL не оптимизирован для хранения бинарных данных
- ❌ Base64 увеличивает размер на ~33%
- ❌ Замедляет запросы к БД
- ❌ Ограничения по размеру (обычно 1GB на строку)
- ❌ Дороже для хранения больших объемов

**Почему Vercel Blob:**
- ✅ Оптимизирован для файлов (CDN, кеширование)
- ✅ Интеграция с Vercel (бесплатно до 1GB)
- ✅ Быстрая загрузка через CDN
- ✅ Автоматическое сжатие
- ✅ Уже есть в зависимостях (`@vercel/blob`)
- ✅ Neon DB хранит только URL (легковесно)

**Альтернативы:**
- AWS S3 (если нужен больший контроль)
- Cloudflare R2 (если нужна совместимость с S3)

**Итог:** Используем **Vercel Blob** для хранения PDF, **Neon DB** только для метаданных (URL, размер, дата создания).

---

## 🎯 Этапы улучшения

### **Этап 1: Миграция на Vercel Blob Storage** ⏱️ 2-3 часа

**Цель:** Переместить хранение PDF из base64 в БД на Vercel Blob.

**Задачи:**
1. ✅ Настроить Vercel Blob Storage
   - Получить `BLOB_READ_WRITE_TOKEN` из Vercel Dashboard
   - Добавить в `.env` и `env.example`

2. ✅ Создать утилиту для работы с Blob
   - `lib/blob-storage.ts` - функции upload/delete/get
   - Обработка ошибок и retry логика

3. ✅ Обновить Inngest функцию
   - Сохранять PDF в Blob вместо base64
   - Сохранять только URL в `course.pdfUrl`

4. ✅ Миграция существующих PDF (опционально)
   - Скрипт для переноса base64 → Blob
   - Обновление существующих записей

**Критерии готовности:**
- [ ] PDF сохраняются в Vercel Blob
- [ ] URL сохраняется в Neon DB
- [ ] Существующие PDF работают
- [ ] Удаление PDF при удалении курса

**Файлы для изменения:**
- `lib/blob-storage.ts` (новый)
- `inngest/functions/pdf-generation.ts`
- `env.example`
- `prisma/schema.prisma` (опционально - добавить `pdfSize`, `pdfCreatedAt`)

---

### **Этап 2: Оптимизация контента через OpenAI** ⏱️ 4-5 часов

**Цель:** Структурировать и оптимизировать контент для компактного PDF (4 страницы).

**Задачи:**
1. ✅ Создать промпт-шаблон для структурирования контента
   - Парсинг `course.content` (plain text/markdown)
   - Форматирование в структурированный HTML
   - Ограничение по объему (4 страницы)

2. ✅ Оптимизация структуры контента
   - Краткое резюме программы (1/2 страницы)
   - Недели в компактном формате (таблицы)
   - Ключевые упражнения (без деталей)
   - Советы по питанию (кратко, если включено)

3. ✅ Создать HTML-шаблон для компактного PDF
   - Минималистичный дизайн
   - Таблицы вместо списков
   - Оптимизированные шрифты и отступы
   - Print-оптимизированные стили

4. ✅ Интеграция с OpenAI для форматирования
   - Использовать `gpt-4o-mini` для структурирования
   - Промпт с ограничением объема
   - Валидация результата

**Критерии готовности:**
- [ ] Контент структурирован и компактен
- [ ] PDF укладывается в 4 страницы
- [ ] Читаемость сохранена
- [ ] Все ключевые данные включены

**Файлы для изменения:**
- `inngest/functions/pdf-generation.ts` (шаг 3 - create-html)
- `lib/pdf-formatter.ts` (новый - утилиты форматирования)
- HTML шаблон в `inngest/functions/pdf-generation.ts`

**Промпт-шаблон:**
```
Ты профессиональный фитнес-тренер. Преобразуй следующий план тренировок в компактный HTML формат для PDF (максимум 4 страницы).

Требования:
- Краткое резюме программы (2-3 предложения)
- Недели в табличном формате
- Ключевые упражнения (название, подходы, повторения)
- Минимум текста, максимум информации
- HTML без markdown разметки
- Оптимизировано для печати

Контент курса:
{course.content}

Опции:
- Недель: {weeks}
- Сессий в неделю: {sessionsPerWeek}
- Типы тренировок: {workoutTypes}
- Целевые мышцы: {targetMuscles}
```

---

### **Этап 3: Оптимизация изображений** ⏱️ 3-4 часа

**Цель:** Использовать существующие изображения, оптимизировать их для PDF.

**Задачи:**
1. ✅ Использовать существующие изображения из курса
   - Парсить `course.images` (JSON массив URL)
   - Не генерировать новые, если уже есть
   - Ограничить до 2-3 изображений для компактности

2. ✅ Скачивание и встраивание изображений
   - Скачивать изображения в base64
   - Встраивать в HTML (data URLs)
   - Оптимизация размера (resize до 800px width)
   - Сжатие (JPEG quality 80%)

3. ✅ Fallback для отсутствующих изображений
   - Если изображений нет - пропустить секцию
   - Или использовать placeholder (опционально)

4. ✅ Обработка ошибок загрузки
   - Retry логика
   - Timeout (5 секунд на изображение)
   - Graceful degradation

**Критерии готовности:**
- [ ] Используются существующие изображения
- [ ] Изображения встраиваются в PDF
- [ ] Размер PDF не превышает разумные пределы
- [ ] Ошибки обрабатываются корректно

**Файлы для изменения:**
- `inngest/functions/pdf-generation.ts` (шаг 2 - generate-images)
- `lib/image-optimizer.ts` (новый - скачивание и оптимизация)

---

### **Этап 4: Улучшение HTML шаблона** ⏱️ 2-3 часа

**Цель:** Создать компактный, профессиональный HTML шаблон для PDF.

**Задачи:**
1. ✅ Компактный дизайн
   - Минимальные отступы
   - Оптимизированные шрифты (12-14px)
   - Таблицы вместо списков
   - Двухколоночный layout где возможно

2. ✅ Print-оптимизация
   - `@media print` стили
   - Правильные разрывы страниц
   - Оптимизированные цвета (черно-белые где возможно)
   - Минимум фоновых изображений

3. ✅ Структура контента
   - Header с логотипом и названием (компактно)
   - Резюме программы (1/2 страницы)
   - Недели в табличном формате
   - Советы по питанию (если включено)
   - Footer с дисклеймером

4. ✅ Использование nutritionAdvice
   - Парсить `course.nutritionAdvice`
   - Форматировать в компактный список
   - Включить только если опция включена

**Критерии готовности:**
- [ ] PDF укладывается в 4 страницы
- [ ] Читаемость сохранена
- [ ] Профессиональный вид
- [ ] Все данные включены

**Файлы для изменения:**
- `inngest/functions/pdf-generation.ts` (HTML шаблон)
- CSS стили в шаблоне

**Пример структуры:**
```html
<!DOCTYPE html>
<html>
<head>
  <style>
    /* Компактные стили */
    body { font-size: 12px; line-height: 1.4; }
    .week-table { width: 100%; border-collapse: collapse; }
    .week-table td { padding: 4px; border: 1px solid #ddd; }
    @media print {
      .page-break { page-break-after: always; }
    }
  </style>
</head>
<body>
  <!-- Header (компактный) -->
  <!-- Резюме (1/2 страницы) -->
  <!-- Недели в таблице -->
  <!-- Питание (если есть) -->
  <!-- Footer -->
</body>
</html>
```

---

### **Этап 5: Оптимизация Puppeteer рендеринга** ⏱️ 2 часа

**Цель:** Улучшить надежность и скорость рендеринга PDF.

**Задачи:**
1. ✅ Улучшенная загрузка изображений
   - Использовать встроенные base64 изображения
   - Убрать фиксированную задержку
   - Проверка загрузки через `page.waitForSelector`

2. ✅ Оптимизация настроек PDF
   - Правильные margins для 4 страниц
   - Оптимизированный формат (A4)
   - Качество сжатия

3. ✅ Обработка ошибок
   - Retry логика для Puppeteer
   - Timeout handling
   - Валидация размера PDF

4. ✅ Логирование и мониторинг
   - Логи времени генерации
   - Размер PDF
   - Количество страниц

**Критерии готовности:**
- [ ] Рендеринг стабилен
- [ ] Время генерации < 30 секунд
- [ ] PDF корректного размера
- [ ] Ошибки обрабатываются

**Файлы для изменения:**
- `inngest/functions/pdf-generation.ts` (шаг 4 - generate-pdf)

---

### **Этап 6: Тестирование и полировка** ⏱️ 3-4 часа

**Цель:** Протестировать все сценарии, исправить баги.

**Задачи:**
1. ✅ Unit тесты
   - Тесты форматирования контента
   - Тесты оптимизации изображений
   - Тесты Blob storage

2. ✅ Интеграционные тесты
   - Полный цикл генерации PDF
   - Различные опции курса
   - Edge cases (нет изображений, нет контента)

3. ✅ E2E тесты
   - Генерация PDF через UI
   - Скачивание PDF
   - Проверка размера и качества

4. ✅ Оптимизация производительности
   - Профилирование времени генерации
   - Оптимизация промптов OpenAI
   - Кеширование где возможно

5. ✅ Документация
   - README для PDF генерации
   - Комментарии в коде
   - Troubleshooting guide

**Критерии готовности:**
- [ ] Все тесты проходят
- [ ] PDF генерируется стабильно
- [ ] Размер PDF < 2MB
- [ ] Время генерации < 30 секунд
- [ ] Документация обновлена

---

## 📊 Метрики успеха

### Производительность
- ⏱️ Время генерации: < 30 секунд
- 📄 Размер PDF: < 2MB
- 📑 Количество страниц: ≤ 4 страницы
- 🖼️ Количество изображений: 0-3

### Качество
- ✅ Читаемость сохранена
- ✅ Все ключевые данные включены
- ✅ Профессиональный вид
- ✅ Корректное форматирование

### Надежность
- ✅ Успешность генерации: > 95%
- ✅ Обработка ошибок
- ✅ Retry логика работает
- ✅ Нет утечек памяти

---

## 🔧 Технические детали

### Структура файлов после улучшений

```
lib/
  ├── blob-storage.ts          # Vercel Blob утилиты
  ├── pdf-formatter.ts         # Форматирование контента
  └── image-optimizer.ts      # Оптимизация изображений

inngest/functions/
  └── pdf-generation.ts        # Обновленная функция

app/api/courses/
  └── generate-pdf/
      └── route.ts             # API endpoint (без изменений)
```

### Environment Variables

```env
# Vercel Blob Storage
BLOB_READ_WRITE_TOKEN="vercel_blob_xxx"

# OpenAI (уже есть)
OPENAI_API_KEY="sk-..."

# Inngest (уже есть)
CHALETCOACHING_INNGEST_EVENT_KEY="..."
CHALETCOACHING_INNGEST_SIGNING_KEY="..."
```

### Prisma Schema (опциональные улучшения)

```prisma
model Course {
  // ... существующие поля
  pdfUrl          String?   // URL в Vercel Blob
  pdfSize         Int?      // Размер в байтах
  pdfCreatedAt    DateTime? // Дата генерации
  pdfPageCount    Int?      // Количество страниц
}
```

---

## 🚀 Порядок выполнения

1. **Этап 1** - Миграция на Blob (критично для производительности)
2. **Этап 2** - Оптимизация контента (критично для ограничения 4 страниц)
3. **Этап 3** - Оптимизация изображений (важно для размера)
4. **Этап 4** - Улучшение HTML (важно для качества)
5. **Этап 5** - Оптимизация Puppeteer (полировка)
6. **Этап 6** - Тестирование (обязательно перед продакшеном)

---

## 📝 Примечания

- Все этапы можно выполнять последовательно
- После каждого этапа - тестирование
- Этапы 1-2 критичны и должны быть выполнены первыми
- Этапы 3-5 можно выполнять параллельно после этапа 2
- Этап 6 обязателен перед деплоем

---

## ❓ FAQ

**Q: Почему не использовать @react-pdf/renderer?**
A: Текущий подход с Puppeteer более гибкий для HTML-контента и лучше интегрируется с OpenAI-сгенерированным контентом.

**Q: Можно ли кешировать PDF?**
A: Да, если курс не изменился, можно использовать существующий PDF. Это уже реализовано в текущем коде.

**Q: Что делать если PDF > 4 страниц?**
A: Усилить промпт для OpenAI, сократить контент, уменьшить количество изображений, использовать более компактный layout.

**Q: Нужна ли миграция существующих PDF?**
A: Опционально. Можно оставить base64 PDF в БД и мигрировать постепенно при следующей генерации.

---

**Дата создания:** 2025-01-XX
**Версия:** 1.0
**Статус:** Готов к реализации

