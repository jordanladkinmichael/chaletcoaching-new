# Настройка Inngest Dashboard для продакшена

## 📋 Обзор

Inngest Dashboard позволяет отслеживать выполнение функций, события, ошибки и логи в реальном времени. Для продакшена используется **Inngest Cloud** (managed service).

---

## 🚀 Шаг 1: Создание аккаунта Inngest

1. Перейдите на [https://www.inngest.com](https://www.inngest.com)
2. Нажмите **"Sign Up"** или **"Get Started"**
3. Войдите через GitHub (рекомендуется) или создайте аккаунт
4. Создайте новый **App** (например, "Chaletcoaching Production")

---

## 🔑 Шаг 2: Получение ключей

После создания App в Inngest Dashboard:

1. Перейдите в **Settings** → **Keys**
2. Скопируйте следующие ключи:

   - **Event Key** (для отправки событий)
     - Используется в `inngest/client.ts`
     - Формат: `your-app-event-key-xxx`
   
   - **Signing Key** (для верификации webhook)
     - Используется в `app/api/inngest/route.ts`
     - Формат: `signkey-xxx`

3. Добавьте их в Vercel Environment Variables:

   ```bash
   CHALETCOACHING_INNGEST_EVENT_KEY=your-event-key-here
   CHALETCOACHING_INNGEST_SIGNING_KEY=signkey-xxx
   ```

---

## 🌐 Шаг 3: Настройка для продакшена

### 3.1. Настройка в Inngest Dashboard

1. В Inngest Dashboard перейдите в **Settings** → **Apps**
2. Найдите ваш App и откройте его
3. В разделе **Sync URL** добавьте URL вашего продакшена:

   ```
   https://your-domain.vercel.app/api/inngest
   ```

4. Нажмите **"Sync"** или **"Test Connection"**

### 3.2. Настройка в Vercel

1. Перейдите в Vercel Dashboard → ваш проект
2. Откройте **Settings** → **Environment Variables**
3. Добавьте/обновите переменные:

   ```env
   CHALETCOACHING_INNGEST_EVENT_KEY=your-event-key
   CHALETCOACHING_INNGEST_SIGNING_KEY=signkey-xxx
   ```

4. Убедитесь, что переменные установлены для **Production** environment
5. Перезапустите deployment (если нужно)

---

## 🔍 Шаг 4: Доступ к Dashboard

### Вариант 1: Через Inngest Cloud Dashboard (Рекомендуется)

1. Войдите в [Inngest Dashboard](https://app.inngest.com)
2. Выберите ваш App
3. Вы увидите:
   - **Events** - все события в реальном времени
   - **Functions** - все зарегистрированные функции
   - **Runs** - история выполнения функций
   - **Logs** - логи выполнения

### Вариант 2: Через Dev Server (только для локальной разработки)

Для локальной разработки можно использовать Inngest Dev Server:

```bash
# Установите Inngest CLI (если еще не установлен)
npm install -g inngest-cli

# Запустите Dev Server
npx inngest-cli@latest dev
```

Dev Server будет доступен по адресу: `http://localhost:8288`

**Важно:** Dev Server работает только локально. Для продакшена используйте Inngest Cloud Dashboard.

---

## 📊 Шаг 5: Отслеживание генерации PDF

### 5.1. Просмотр событий

1. В Inngest Dashboard перейдите в **Events**
2. Найдите событие `pdf/generate`
3. Вы увидите:
   - Время события
   - Данные события (courseId, userId)
   - Статус обработки

### 5.2. Просмотр выполнения функций

1. Перейдите в **Functions** → **generate-pdf**
2. Вы увидите:
   - Все запуски функции
   - Статус каждого шага:
     - `get-course` - получение курса из БД
     - `optimize-images` - оптимизация изображений
     - `format-content` - форматирование контента
     - `create-html` - создание HTML
     - `generate-pdf` - генерация PDF через Puppeteer
     - `upload-pdf-to-blob` - загрузка в Vercel Blob
     - `save-pdf-url` - сохранение URL в БД
   - Время выполнения каждого шага
   - Ошибки (если есть)

### 5.3. Просмотр логов

1. Откройте конкретный Run функции
2. В разделе **Logs** вы увидите:
   - Все console.log из функции
   - Ошибки и stack traces
   - Время выполнения

### 5.4. Фильтрация и поиск

- **По статусу:** Success, Failed, Running
- **По времени:** Последний час, день, неделя
- **По userId или courseId:** Используйте поиск

---

## 🐛 Шаг 6: Отладка проблем

### Проблема: Функция не запускается

**Проверьте:**
1. ✅ Event Key установлен в Vercel
2. ✅ Signing Key установлен в Vercel
3. ✅ Sync URL правильный в Inngest Dashboard
4. ✅ Функция зарегистрирована в `app/api/inngest/route.ts`
5. ✅ Событие отправляется правильно (проверьте логи в Vercel)

### Проблема: Функция падает с ошибкой

**Проверьте:**
1. Откройте Run в Inngest Dashboard
2. Посмотрите на шаг, где произошла ошибка
3. Проверьте логи этого шага
4. Проверьте Environment Variables в Vercel

### Проблема: Dashboard не показывает события

**Проверьте:**
1. ✅ Sync URL правильный
2. ✅ Signing Key совпадает в Vercel и Inngest
3. ✅ Функция правильно экспортирована
4. ✅ Событие отправляется (проверьте Network tab в браузере)

---

## 📝 Примеры использования Dashboard

### Пример 1: Отслеживание конкретной генерации PDF

1. Пользователь нажимает "Generate PDF" в Dashboard
2. В Inngest Dashboard → **Events** появится новое событие `pdf/generate`
3. Откройте событие → увидите данные: `{ courseId: "...", userId: "..." }`
4. Перейдите в **Runs** → увидите выполнение функции `generate-pdf`
5. Откройте Run → увидите все шаги и их статус

### Пример 2: Поиск проблемных генераций

1. В **Functions** → **generate-pdf** → **Failed**
2. Откройте failed run
3. Посмотрите на шаг, где произошла ошибка
4. Проверьте логи этого шага
5. Исправьте проблему и перезапустите

### Пример 3: Мониторинг производительности

1. В **Functions** → **generate-pdf** → **Runs**
2. Посмотрите на время выполнения каждого шага
3. Найдите узкие места (долгие шаги)
4. Оптимизируйте соответствующий код

---

## 🔐 Безопасность

### Важно:

1. **Никогда не коммитьте ключи в Git**
   - Используйте `.env` для локальной разработки
   - Используйте Vercel Environment Variables для продакшена

2. **Signing Key должен быть секретным**
   - Используется для верификации webhook от Inngest
   - Не должен быть доступен публично

3. **Event Key можно использовать публично**
   - Но лучше хранить в Environment Variables
   - Используется для отправки событий

---

## 📚 Полезные ссылки

- [Inngest Documentation](https://www.inngest.com/docs)
- [Inngest Dashboard](https://app.inngest.com)
- [Inngest Next.js Guide](https://www.inngest.com/docs/quick-start/nextjs)
- [Inngest Functions Guide](https://www.inngest.com/docs/functions)

---

## ✅ Чек-лист настройки

- [ ] Создан аккаунт в Inngest
- [ ] Создан App в Inngest Dashboard
- [ ] Получены Event Key и Signing Key
- [ ] Ключи добавлены в Vercel Environment Variables
- [ ] Sync URL настроен в Inngest Dashboard
- [ ] Тестовая генерация PDF прошла успешно
- [ ] События видны в Inngest Dashboard
- [ ] Функции видны в Inngest Dashboard
- [ ] Логи доступны для просмотра

---

**Дата создания:** 2025-01-XX
**Статус:** Готово к использованию

